# 03 [AWS Lambda](https://aws.amazon.com/lambda/) + Alternative Indexes

This sample is auto-generated by `tools/generate-samples.cjs` from the plugin generators in this repo.

## Generated with

```bash
pnpm create nx-workspace@latest acme-lambda --preset=ts --packageManager=pnpm --nxCloud=skip --interactive=false
cd acme-lambda
pnpm add -D @mgwilt/nx-uv@beta
pnpm nx g @mgwilt/nx-uv:workspace --name=acme-lambda --membersGlob='packages/py/*'
pnpm nx g @mgwilt/nx-uv:project lambda-api --projectType=app --directory=packages/py
pnpm nx g @mgwilt/nx-uv:integration --template=aws-lambda --project=lambda-api
pnpm nx g @mgwilt/nx-uv:integration --template=alternative-indexes --project=lambda-api
cd packages/py/lambda-api
uv add boto3
uv add --dev pytest ruff
cd ../../..
```

## Run targets

```bash
pnpm nx run lambda-api:sync
pnpm nx run lambda-api:test
pnpm nx run lambda-api:build
```
