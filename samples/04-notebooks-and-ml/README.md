# 04 [Jupyter](https://jupyter.org/) + [marimo](https://marimo.io/) + [Coiled](https://coiled.io/) + [PyTorch](https://pytorch.org/)

This sample is auto-generated by `tools/generate-samples.cjs` from the plugin generators in this repo.

## Generated with

```bash
pnpm create nx-workspace@latest acme-ml --preset=ts --packageManager=pnpm --nxCloud=skip --interactive=false
cd acme-ml
pnpm add -D @mgwilt/nx-uv@beta
pnpm nx g @mgwilt/nx-uv:workspace --name=acme-ml --membersGlob='packages/py/*'
pnpm nx g @mgwilt/nx-uv:project lab --projectType=lib --directory=packages/py
pnpm nx g @mgwilt/nx-uv:integration --template=jupyter --project=lab
pnpm nx g @mgwilt/nx-uv:integration --template=marimo --project=lab
pnpm nx g @mgwilt/nx-uv:integration --template=coiled --project=lab
pnpm nx g @mgwilt/nx-uv:integration --template=pytorch --project=lab
cd packages/py/lab
uv add pandas pyarrow torch
uv add --dev pytest ruff ipykernel marimo
cd ../../..
```

## Run targets

```bash
pnpm nx run lab:sync
pnpm nx run lab:test
pnpm nx run lab:build
```
