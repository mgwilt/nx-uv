# 06 [PyTorch](https://pytorch.org/) CUDA + NVIDIA Inference [Docker](https://www.docker.com/)

This sample is auto-generated by `tools/generate-samples.cjs` from the plugin generators in this repo.

## Generated with

```bash
pnpm create nx-workspace@latest acme-cuda --preset=ts --packageManager=pnpm --nxCloud=skip --interactive=false
cd acme-cuda
pnpm add -D @mgwilt/nx-uv@beta
pnpm nx g @mgwilt/nx-uv:workspace --name=acme-cuda --membersGlob='packages/py/*'
pnpm nx g @mgwilt/nx-uv:project inference --projectType=app --directory=packages/py
pnpm nx g @mgwilt/nx-uv:integration --template=jupyter --project=inference
pnpm nx g @mgwilt/nx-uv:integration --template=pytorch --project=inference --backend=cuda --includeNotebook=true --includeDocker=true
cd packages/py/inference
uv add "torch==2.5.1" "torchvision==0.20.1" "torchaudio==2.5.1"
uv add --dev pytest ruff ipykernel
cd ../../..
```

## Run targets

```bash
pnpm nx run inference:sync
pnpm nx run inference:test
pnpm nx run inference:build
```
