[project]
name = "rocm-lab"
version = "0.1.0"
description = "Python lib generated by @mgwilt/nx-uv"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rocm_lab"]
